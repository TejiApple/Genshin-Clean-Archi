<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.genshininfoapp">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.GenshinInfoApp">
        <activity android:name=".ui.characters.CharacterDetailsActivity"></activity>
        <activity
            android:name=".ui.weapons.WeaponsActivity"
            android:label="Weapons"
            android:windowSoftInputMode="stateHidden" />
        <activity
            android:name=".ui.domains.ArtifactsActivity"
            android:label="Artifacts"
            android:windowSoftInputMode="stateHidden" />
        <activity
            android:name=".ui.characters.CharactersActivity"
            android:label="Characters"
            android:windowSoftInputMode="stateHidden" />
        <activity android:name=".ui.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>